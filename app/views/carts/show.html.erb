<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Panier</title>
  </head>
  <body>
    <div class="container"> 
    
    <div class="card mb-3" style="max-width: 800px;">
      <div class="row g-0">
          <div class="card-body " style="max-height: 100%;">
            <h1 class="card-title"> Mon panier </h1>
              <div class="card-text">
              <% @cart.items.each do |item|%>
                <div class="card mb-3" style="max-width: 800px;">
                  <div class="row g-0">
                    <div class="col-md-4">
                      <img src="<%=item.image_url%>" class="img-fluid rounded-start" alt="...">
                    </div>
                    <div class="col-md-6">
                      <div class="card-body">
                        <h5 class="card-title"><%= item.title %></h5>
                        <p class="card-text"><%= item.description %> </p>
                      </div>
                    </div>
                    <div class="col-md-2">
                      <div class="card-body">
                        <h5 class="card-title">Prix</h5>
                        <p class="card-text"><%= item.price%>€ </p>
                      </div>
                      <%= link_to "Supprimer du panier", cart_path(item.id), :method => :delete %>
                    </div>
                  </div>
                </div> 
                <% @total += item.price %>
                <%end%>
                </div>
              </div>
            </div>
            <div class="card-text">
            <h1 class:"text-align-left">TOTAL : <%= @total%>€ </h1>
            </div>

        </div>
      </div>
    </div>
  </body>
</html>