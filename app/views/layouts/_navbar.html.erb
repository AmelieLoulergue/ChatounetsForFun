<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%=root_path%>">🐱 Chatounets</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if !user_signed_in? %>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
            Se connecter/S'inscrire
          </a>
          <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1" onclick="play()">
            <li><%= link_to 'Connexion', new_user_session_path, class:"dropdown-item" %></li>
            <li><%= link_to 'Inscription', new_user_registration_path, class:"dropdown-item" %></li>
          </ul>
        </div>
      <% else %>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false" onclick="play()">
            Mon compte
          </a>
          <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
            <li><%= link_to 'Mon profil', user_path(current_user.id), class:"dropdown-item" %></li>
            <li><%= link_to 'Mes commandes', user_orders_path(current_user.id), class:"dropdown-item"%></li>
            <li><%= link_to 'Deconnexion', destroy_user_session_path, :method => :delete, class: "dropdown-item" %></li>
          </ul>
        </div>
        <li><a class="nav-link" href="<%= user_cart_path(user_id:current_user.id, id:current_user.cart.id) %>" ><i class="fas fa-shopping-cart"></i><span class="badge text-success" id="total-cart"><%=@total_cart%></span></a></li>
      <% end %>
    </ul>
  </div>
</nav>

<audio id="audio" src="https://lasonotheque.org/UPLOAD/wav/1890.wav"></audio>
